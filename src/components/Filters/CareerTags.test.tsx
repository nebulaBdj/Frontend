// // src/components/ProgramTag.test.tsx

// import { render, screen } from '@testing-library/react';
// import '@testing-library/jest-dom';
// import ProgramTag from './FilterTag';

// describe('ProgramTag Component', () => {
//   test('renders with given label and color', () => {
//     render(<ProgramTag color="bg-blue-500" label="성장" />);

//     // Check if the label is rendered
//     expect(screen.getByText('성장')).toBeInTheDocument();

//     // Check if the color class is applied
//     const colorElement = screen.getByRole('img');
//     expect(colorElement).toHaveClass('bg-blue-500');
//   });
// });
